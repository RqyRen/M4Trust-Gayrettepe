# M4Trust AI Service - runtime bağımlılıkları
#
# Exact-pinned (Berke review #8, 18 Temmuz 2026): sürümsüz requirements.txt
# aynı commit'in farklı tarihlerde build edilmesinde farklı FastAPI/Pydantic/
# OpenAI/Redis/Pika/JSON Schema sürümleri getirebiliyordu. Asagidaki dogrudan
# bagimliliklar `pip install -r requirements.txt` (temiz bir venv'de) ile
# cozulup `pip freeze` ile sabitlendi; production image (Dockerfile) da ayni
# dosyayi kullanir, boylece ai-api/ai-worker ayni surumleri paylasir.
#
# Yeniden uretmek icin: temiz bir venv'de `pip install -r requirements.txt`
# (bu dosyanin "Dogrudan bagimliliklar" bolumundeki surumsuz halini kullanarak),
# sonra `pip freeze` ile sonucu buraya kopyala.
#
# Hash dogrulama (`pip install --require-hashes`) BILEREK eklenmedi: Python
# 3.14 cok yeni oldugu icin bazi paketler (numpy, lxml, opencv, pydantic-core,
# rpds-py) henuz her platform icin manylinux wheel yayinlamamis olabilir
# (Dockerfile builder asamasindaki build-essential source-build fallback'i
# bu yuzden var). Hash'ler yalniz gercekte indirilen platform-spesifik
# wheel'lerden uretilebilir; Windows'ta uretilen hash'ler Linux build'inde
# gecersiz olur ve build'i kirar. Exact version pin + Docker base image
# digest pin (Dockerfile) su an icin yeterli guvence sagliyor.

# --- Dogrudan bagimliliklar ---

# Web / ai-api (operasyonel endpoint'ler)
fastapi==0.139.2
uvicorn[standard]==0.51.0

# Messaging (RabbitMQ - ADR-002)
pika==1.4.1

# Idempotency job store: paylasimli/kalici storage (coklu worker replica'sinda
# process-local bellek yeterli degil - bkz. app/common/idempotency.py)
redis==8.0.1

# Kaynak indirme: kisa omurlu presigned URL (ADR-001 §6)
# NOT: object storage'a genel SDK erisimi YOK; sadece HTTP GET.
httpx==0.28.1

# Document extraction: metin cikarimi + LLM (ADR-002 §3.1, ADR-004 §16)
pypdf==6.14.2
python-docx==1.2.0
openai==2.46.0

# OCR fallback (ADR-002 §3.1 "gerektiginde OCR")
# NOT: tesseract-ocr binary + tur/eng dil paketleri host/image'da ayrica kurulmali.
pytesseract==0.3.13
PyMuPDF==1.28.0
Pillow==12.3.0

# Video analysis: frame ornekleme + Roboflow REST cagrisi (ADR-002 §3.2, ADR-004 §16)
# NOT: resmi inference-sdk paketi bu ortamin Python surumunu desteklemiyor;
# dogrudan httpx ile REST API'ye baglanilir (ADR-007 §15 minimum dependency).
opencv-python-headless==5.0.0.93

# Contract validation (ADR-002 §24)
# NOT: referencing acikca kullanilir (schema_store.py) - deprecated
# RefResolver'in scope-stack bug'ini onlemek icin modern Registry API'si.
jsonschema==4.26.0
referencing==0.37.0
PyYAML==6.0.3

# Config
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Legal RAG: sozlesme kurallarini ilgili kanun maddeleriyle baglamlandirma
# (app/pipeline/document_extraction/legal_rag.py, embedding_provider.py).
# Yerel BAAI/bge-m3 modeli kullanilir (ucretli bir API'ye bagimlilik yerine
# agir ama ucretsiz yerel bir model tercih edildi). Vektor depolama icin ayri
# bir veritabani (ChromaDB vb.) YOK -- kulliyat kucuk (~900 parca), numpy
# (zaten transitive bagimlilik) ile in-memory cosine similarity yeterli.
FlagEmbedding==1.4.0

# --- Transitive bagimliliklar (yukaridakiler tarafindan otomatik cekilir;
# elle eklenmez, sadece `pip freeze` cikisiyla senkron tutulur) ---
accelerate==1.14.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.1
aiosignal==1.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.6.17
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
datasets==5.0.0
dill==0.4.1
distro==1.9.0
filelock==3.31.0
frozenlist==1.8.0
fsspec==2026.4.0
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httptools==0.8.0
huggingface_hub==1.24.0
idna==3.18
ir_datasets==0.6.3
Jinja2==3.1.6
jiter==0.16.0
joblib==1.5.3
jsonschema-specifications==2025.9.1
lxml==6.1.1
lz4==4.4.5
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
multidict==6.7.1
multiprocess==0.70.19
narwhals==2.24.0
networkx==3.6.1
numpy==2.5.1
packaging==26.2
pandas==3.0.3
peft==0.19.1
propcache==0.5.2
protobuf==7.35.1
psutil==7.2.2
pyarrow==25.0.0
pydantic_core==2.46.4
python-dateutil==2.9.0.post0
regex==2026.7.10
requests==2.34.2
rich==15.0.0
rpds-py==2026.6.3
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.18.0
sentence-transformers==5.6.0
sentencepiece==0.2.2
setuptools==83.0.0
shellingham==1.5.4
six==1.17.0
sniffio==1.3.1
starlette==1.3.1
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.22.2
torch==2.13.0
tqdm==4.69.0
transformers==5.14.1
typer==0.27.0
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
watchfiles==1.2.0
xxhash==3.8.1
yarl==1.24.2
websockets==16.1.1
